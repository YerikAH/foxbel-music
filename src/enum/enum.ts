export enum ResProps {
  albums = 'albums',
  artists = 'artists',
  playlists = 'playlists',
  podcasts = 'podcasts',
  tracks = 'tracks',
}
export enum AllOptions{
  recent = 'Recientes',
  artist = 'Artistas',
  albums = 'Albums',
  podcast = 'Podcast'
}

export enum PathRoutes{
  recent = '/',
  artist = '/artists',
  albums = '/albums',
  podcast = '/podcasts'
}